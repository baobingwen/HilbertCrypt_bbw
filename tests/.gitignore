# tests/ 目录自己的忽略规则。放在这里而不是根 .gitignore，是为了让"测试代码进版本库、
# 测试产物不进版本库"这件事就地可读。
#
# 需要提交的：所有 *.mjs 测试脚本、README.md、以及体积很小、跨语言比对用得上的参考数据。
# 不需要提交的：下面这些运行期产物。

# 测试运行产物：沙箱目录、生成的测试图、体积排查报告、导出的历史残留图片
out/

# 临时文件
*.tmp
*.bak
*.log

# 编辑器 / 系统
.DS_Store
Thumbs.db
