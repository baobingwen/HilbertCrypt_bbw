## 2025-04-12

### 临时思路

剥离算法功能，重新设计交互部分

## 2025-04-13

1. 搬运下来源代码，消化并实现本地Nodejs化。
2. 改进html代码中jpg损失的问题。
3. 上传html子项目到[baobingwen.github.io/tools/GilbertCrypt](https://baobingwen.github.io/tools/GilbertCrypt/)，实现线上浏览器通用。

## 2025-04-14

1. 加入拖拽图片上传图片的功能

### 后续计划

1. 目前算法固定，后续可以实现算法上的加密混淆，RSA-2048加密等等，对像素偏移量进行加密。
2. 明确当前项目优势和劣势，目的及其改进方向。

## 2025-04-20

1. 上线v3-beta-4.2版本，重新设计网页前端设计
2. 重新设计等待中为进度条

## 2025-04-23

1. 重新设计Py版本代码结构，把算法和应用分离开
2. 使用c++的各种库重新构建混淆体系，为了加速和测试

## 2025-04-24

1. 开始搭建本地c++环境
2. 搭建环境完毕
3. 初代程序测试，发现算法只做到了不完备的hilbert曲线生成

## 2025-04-25

1. C++版本基本构建完毕
2. 完善命令行输出提示
3. 兼容更多图片格式
4. 做相对完善的测试报告

## 2025-04-26

1. 尝试构建静态C++程序，但是一直报错，没能成功
2. 使用msys2运用mingw64编译器，通过CMake，成功构建C++ Release win v1.0.0 .exe版本

## 2025-04-27

1. 修复图片转化输出通道对应的问题，确保图像输出正确
2. 发布Cpp-Release-v1.0.0
3. 增强日志输出，兼容多线程日志输出
4. 发布cpp-Release-v1.0.1

## 2025-04-28

1. 程序日志输出合并“正在复制像素”和“进度”信息
2. 错误提示中，添加文件夹中没有支持格式的图片并提示当前支持的格式。

## 2025-05-05

1. 正式开始开发微信小程序版
2. 做了简化Web版，用于对接wx小程序版

## 2025-05-11

1. Rust源WebAssmbly提升frontend-web端in dev.

## 2025-05-12

1. WebAssembly验证完成，实装部分操作图像，性能瓶颈：转换数据格式
2. 尝试全WebAssembly操作图像，牺牲实时进度功能

## 2025-05-13

1. 完成全像素操作WebAssembly化，实现速度飞跃式提升
2. 整合程序资源，规划开发板、测试版、Release版多板块分化，便于开发和发布
3. 临时上线`v2-alpha`版，开放多端测试

## 2026-09-12（项目复盘与维护：三端算法统一 + 工程化）

隔了一年多重新捡起这个项目，先做了一次完整体检，然后把发现的问题逐个修掉。
这次改动的核心目标是**消除"三份实现、两套曲线"的历史债**，并补上可复现的构建与验证。

### 体检发现的主要问题

1. **Python 与 C++/Web 用的不是同一条曲线**：Python 是"2^n 方阵希尔伯特曲线 + 按宽高过滤"，
   C++/Web 是广义希尔伯特曲线。README 里"混淆结果暂不跨语言兼容"从来没被验证过，
   实际是"永远不可能兼容"。
2. **WASM 产物不可复现**：仓库里只有 `web/tmp/` 下两份临时备份，且与发布产物的 wasm 不一致
   （备份里还留着 `println!`），也就是说浏览器端核心是"来源不明的二进制"。
3. **`web/src/worker.js` 有一个未提交的修复本身带 bug**：内层 `const gilbert` 遮蔽外层
   `let gilbert`，导致 `finally` 里的内存释放是死代码。
4. **Web 与命令行的默认偏移量公式不同**（`φ×(total-1)` vs `φ×total`）。
5. **C++ 端**：`int64_t total = width * height` 先按 int 相乘（3 亿像素的图会溢出）、
   "一个文件一个线程"、直接覆盖原文件（失败会留下半张图）、有损格式不提示。
6. 杂项：`web/src/*.txt` 是源文件的重复副本、`devlog.md` 与 `js/`（算法源头）被 gitignore 排除、
   `.gitignore` 把 `tests/` 和 `*.spec` 也忽略了。

### 本次改动

1. **补齐 WASM 源码**：新建 `web/rs`（Rust crate），用 `wasm-bindgen =0.2.100` 锁死版本，
   按旧 blob 的导出表反推出 `lib.rs`，重新构建出行为等价的产物（导出表、曲线坐标、
   全部偏移/方向的像素输出、非法入参行为逐项比对一致）。
   同时把"另开一整份缓冲区"的像素重排改成原地循环置换，峰值内存减半。
2. **Python 端对齐**：新增 `Python/src/gilbert_core.py`（同一条广义曲线 + 同公式偏移量 +
   统一 RGBA 语义），重写 `cli.py`；旧实现归档到 `Python/legacy/`。
   中途试过"方阵曲线向量化捷径"，被单测抓出非方形尺寸点序不一致（正是旧版不兼容的根因），已删除。
3. **Web 端修复**：修掉遮蔽 bug 与内存泄漏、统一默认偏移量为 `φ×total`、删除已废弃的
   `retry` 分块协议分支、补上缓冲区长度与画布尺寸校验、所有失败路径都恢复 UI 状态。
4. **C++ 端加固**：全程 `int64_t`、固定大小线程池（`-j`）、临时文件 + 改名原子写入、
   曲线双射校验、`-o/--offset` 显式偏移量、有损格式警告与质量参数、原地置换（`--out-of-place` 保留对照）。
5. **建立测试体系**：`tests/` 下四端逐字节比对（跨语言）、wasm 新旧二进制比对、
   Web 端契约 + 用未改动的 worker.js 跑真实尺寸、端到端 CLI（含 `--offset/--jobs` 行为）、
   Rust/Python 单元测试，以及性能基线 `tests/bench.mjs`。
   一键入口：`node tests/run_all.mjs`。
6. **版本管理整理**：`.gitignore` 精确化（把 `devlog.md`、`js/`、`Python/tests/`、`*.spec`
   纳入版本控制，继续忽略大体积素材与构建产物），删掉重复副本，按主题拆成多个提交。

### 遗留与后续想法

> 这些已经整理成可执行的任务清单，见 [`ROADMAP.md`](ROADMAP.md)
> （方案 B：跨平台支持；方案 C：密钥模式 / 整图 AEAD / 稀疏像素加密）。下面保留当初的原始想法。

1. 混淆仍是无密钥的确定性置换，`devlog` 里 2025-04-14 提到的"对偏移量做 RSA 加密"还没做；
   真要防破解，应该把曲线参数也纳入密钥。
2. Web 端仍是单张图、无进度（v2 起 WASM 一次性处理），若要处理手机上的大图需要分块 + 进度协议。
3. 仅 Windows 验证过；`windows.h`/`psapi` 依赖与 `Cpp/build.ps1` 的 MSYS2 路径都写死了。
4. `web/versions/` 里的历史版本只是存档，不再维护；若线上 Pages 还挂着旧版，
   建议换成 `web/src` 的新版本并同步说明偏移量变更。

### 仓库瘦身（同日追加）

体检时发现"仓库 728MB、但工作区只有 9MB"，用 `tests/repo_bloat*.mjs` 逐层盘查后确认：
20 个提交里从没提交过这些大文件，726.6MB 全是**不可达的松散对象**——早年在 PyInstaller
打包流程里 `git add` 过 `build/`、`*.exe`、`*.7z` 与测试大图，而对应的提交后来被改写/丢弃，
对象却一直留在对象库里（`.gitignore` 是后来才加的，管不了已入对象库的东西）：

- PyInstaller 产物约 536MB：`bbw_tphx_with_opencv.exe/.pkg/.7z`（121+121+120MB）、
  `bbw_tphx`、`bbw_tphx_NumPy` 各自的 exe/pkg/7z，以及 3 个 `PYZ-00.pyz`
- 测试大图约 130MB：4 张 4096×4096、2160×3840 竖版、3000×3583 等
- 旧版 `unifont-all.ttf` 20.2MB

处理方式：全部 89 个对象导出到 `archive/2026-09-12-orphaned-objects/`（含 `MANIFEST.md`
与逐个 `git hash-object` 复核记录，校验 89/89 通过）作为纪念，随后 `git gc --prune=now`。
结果：`.git` 728.5MB → 9.6MB，clone 体积 737MB → 18.7MB，提交、标签、测试全部完好。

